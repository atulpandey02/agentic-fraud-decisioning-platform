# Core
python-dotenv==1.0.0
faker==24.0.0

# Kafka
kafka-python==2.0.2

# Snowflake
snowflake-connector-python==3.7.0

# Redis (Phase 1)
redis==5.0.1

# Spark (submit jobs — not installed locally, runs in container)
pyspark==3.5.0

# Agents (Phase 3+)
langchain==0.1.20
langgraph==0.0.55
langchain-groq==0.1.3
langsmith==0.1.63

# BI Dashboard (Phase 7)
streamlit==1.32.0
plotly==5.20.0

# Utils
uuid==1.30