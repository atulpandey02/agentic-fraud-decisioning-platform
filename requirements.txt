# =============================================================
# CORE — Phase 1 (data generation, Kafka, Snowflake, Redis)
# Install with: pip install -r requirements.txt
# =============================================================
python-dotenv==1.0.0
faker==24.0.0
confluent-kafka==2.4.0
snowflake-connector-python==3.7.0
redis==5.0.1
pyspark==3.5.0
boto3==1.34.100
pyarrow==15.0.2

# =============================================================
# AGENTS — Phase 3+ (install separately when we get there)
# pip install -r requirements-agents.txt
# Kept apart from core deps because langchain/langgraph version
# pins conflict with each other and aren't needed until Phase 3
# =============================================================

# =============================================================
# BI DASHBOARD — Phase 7 (install separately when we get there)
# pip install -r requirements-bi.txt
# =============================================================